# pdi-fundraising
## A web application for ordering products in a fundraising campaign

To build docker image, run docker_build.bat. 

To build docker image and push it to docker hub, run docker_build_push.bat (note: docker hub account must be setup).

To run it locally, run docker_run.bat.